import React from 'react';
export const Input = (props) => (
  <input {...props} style={{ padding: '8px', borderRadius: '6px', border: '1px solid #333', background: '#222', color: '#fff', margin: '4px 0' }} />
);
export default Input;
